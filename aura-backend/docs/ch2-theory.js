const { h1, h2, h3, h4, p, bullet, numberedItem, spacer, pageBreak, makeTable, figureCaption } = require('./report-helpers');

module.exports = function chapter2() {
  return [
    h1('CHƯƠNG 2: CƠ SỞ LÝ THUYẾT'),
    pageBreak(),

    // ===== 2.1 =====
    h2('2.1. Cơ sở lý thuyết'),

    h3('2.1.1. HyperText Markup Language (HTML)'),
    p('**HTML (HyperText Markup Language)** là ngôn ngữ đánh dấu siêu văn bản tiêu chuẩn được sử dụng để tạo cấu trúc và nội dung cho các trang web. HTML sử dụng hệ thống các thẻ (tag) để định nghĩa các phần tử trên trang như tiêu đề, đoạn văn, hình ảnh, liên kết, bảng biểu và biểu mẫu.', { indent: true }),
    p('Trong đồ án AuraPC, nhóm sử dụng **HTML5** — phiên bản mới nhất của HTML với nhiều cải tiến quan trọng:', { indent: true }),
    bullet('**Semantic Elements:** Các thẻ ngữ nghĩa như <header>, <nav>, <main>, <section>, <article>, <footer> giúp cấu trúc trang web rõ ràng và tối ưu cho SEO.'),
    bullet('**Form Enhancements:** Hỗ trợ các kiểu input mới (email, tel, date, number) với validation tích hợp, được sử dụng trong các form đăng ký, đặt hàng và quản lý tài khoản.'),
    bullet('**Canvas và WebGL:** Hỗ trợ đồ họa 2D/3D trực tiếp trên trình duyệt, là nền tảng cho Three.js hiển thị mô hình 3D trên trang chủ AuraPC.'),
    bullet('**Web Storage:** localStorage và sessionStorage cho phép lưu trữ dữ liệu phía client, được AuraPC sử dụng để lưu JWT token, giỏ hàng cho khách vãng lai và sản phẩm đã xem gần đây.'),
    spacer(),

    h3('2.1.2. Cascading Style Sheets (CSS)'),
    p('**CSS (Cascading Style Sheets)** là ngôn ngữ tạo kiểu dáng dùng để mô tả cách trình bày các phần tử HTML trên trang web. CSS tách biệt nội dung (HTML) khỏi trình bày (style), cho phép kiểm soát bố cục, màu sắc, typography, animation và responsive design.', { indent: true }),
    p('Trong đồ án AuraPC, nhóm áp dụng các kỹ thuật CSS hiện đại:', { indent: true }),
    bullet('**CSS3 Features:** Flexbox và Grid Layout để xây dựng bố cục responsive, CSS Variables (Custom Properties) cho hệ thống design token, CSS Transitions và Animations cho hiệu ứng chuyển động mượt mà.'),
    bullet('**Responsive Design:** Sử dụng Media Queries để tối ưu giao diện cho desktop, tablet và mobile. AuraPC hỗ trợ đầy đủ các kích thước màn hình phổ biến.'),
    bullet('**Component-scoped CSS:** Angular hỗ trợ CSS scope theo component (ViewEncapsulation), giúp tránh xung đột class name giữa các component.'),
    bullet('**CSS Variables cho Dark/Light Theme:** Admin Panel sử dụng CSS Variables kết hợp với ThemeService để chuyển đổi giữa giao diện sáng và tối.'),
    spacer(),

    h3('2.1.3. JavaScript và TypeScript'),
    p('**JavaScript (JS)** là ngôn ngữ lập trình kịch bản phía client (và server với Node.js), cho phép tạo tương tác động trên trang web. JavaScript là ngôn ngữ đa mô hình: hướng đối tượng, hàm (functional) và hướng sự kiện (event-driven).', { indent: true }),
    p('**TypeScript (TS)** là ngôn ngữ lập trình mã nguồn mở do Microsoft phát triển, là superset của JavaScript. TypeScript bổ sung hệ thống kiểu tĩnh (static typing) cho JavaScript, giúp phát hiện lỗi sớm trong quá trình phát triển, tăng cường khả năng bảo trì mã nguồn và hỗ trợ IntelliSense trong IDE.', { indent: true }),
    p('Trong đồ án AuraPC:', { indent: true }),
    bullet('**Frontend (Angular):** Sử dụng TypeScript 5.9 với chế độ strict mode, bao gồm: strict null checks, noImplicitReturns, noImplicitOverride và strictTemplates.'),
    bullet('**Backend (Node.js):** Sử dụng JavaScript ES2022+ với các tính năng hiện đại: async/await, optional chaining, nullish coalescing, destructuring.'),
    bullet('**Lợi ích trong dự án:** TypeScript giúp giảm thiểu lỗi runtime, cung cấp autocomplete chính xác cho các interface phức tạp (Product, Order, User) và cải thiện khả năng refactoring.'),
    spacer(),

    h3('2.1.4. HTTP và Cơ chế REST API'),
    p('**HTTP (HyperText Transfer Protocol)** là giao thức truyền tải siêu văn bản, nền tảng của việc trao đổi dữ liệu trên World Wide Web. HTTP hoạt động theo mô hình request-response: client gửi yêu cầu (request) đến server và server trả về phản hồi (response).', { indent: true }),
    p('**REST (Representational State Transfer)** là kiến trúc phần mềm được Roy Fielding đề xuất năm 2000, định nghĩa tập hợp các ràng buộc để thiết kế web service. RESTful API sử dụng các phương thức HTTP tiêu chuẩn để thực hiện các thao tác CRUD:', { indent: true }),

    makeTable(
      [2000, 2800, 4560],
      ['Phương thức HTTP', 'Thao tác CRUD', 'Ví dụ trong AuraPC'],
      [
        ['GET', 'Read (Đọc dữ liệu)', 'GET /api/products — Lấy danh sách sản phẩm'],
        ['POST', 'Create (Tạo mới)', 'POST /api/orders — Tạo đơn hàng mới'],
        ['PUT', 'Update (Cập nhật toàn bộ)', 'PUT /api/auth/profile — Cập nhật hồ sơ người dùng'],
        ['PATCH', 'Partial Update (Cập nhật một phần)', 'PATCH /api/admin/orders/:id — Cập nhật trạng thái đơn'],
        ['DELETE', 'Delete (Xóa)', 'DELETE /api/cart/remove — Xóa sản phẩm khỏi giỏ hàng'],
      ]
    ),
    spacer(),
    p('AuraPC backend cung cấp hơn 80 RESTful API endpoints được tổ chức theo nhóm chức năng: /api/auth, /api/products, /api/orders, /api/cart, /api/payment, /api/reviews, /api/hub, /api/chat, /api/support, /api/admin/*, cho phép frontend giao tiếp với server một cách có cấu trúc và nhất quán.', { indent: true }),
    spacer(),

    h3('2.1.5. JSON Web Token (JWT)'),
    p('**JSON Web Token (JWT)** là tiêu chuẩn mở (RFC 7519) định nghĩa cách truyền tải thông tin một cách an toàn giữa các bên dưới dạng đối tượng JSON. JWT được ký số (digitally signed) bằng thuật toán HMAC hoặc RSA, đảm bảo tính toàn vẹn và xác thực của dữ liệu.', { indent: true }),
    p('Cấu trúc JWT gồm 3 phần, phân tách bởi dấu chấm (.):', { indent: true }),
    numberedItem('**Header:** Chứa loại token (JWT) và thuật toán ký (HS256).'),
    numberedItem('**Payload:** Chứa các claims — thông tin về người dùng (userId, phoneNumber) hoặc admin (adminId, isAdmin).'),
    numberedItem('**Signature:** Chữ ký số được tạo bằng cách mã hóa Header + Payload với JWT_SECRET, đảm bảo token không bị giả mạo.'),
    p('Trong hệ thống AuraPC, JWT được sử dụng như sau:', { indent: true }),
    bullet('**Xác thực người dùng:** Sau khi xác thực OTP thành công, server tạo JWT (thời hạn 7 ngày) chứa userId và phoneNumber, trả về cho client.'),
    bullet('**Xác thực admin:** Admin đăng nhập bằng email/password, server trả JWT chứa adminId và isAdmin: true.'),
    bullet('**Lưu trữ phía client:** JWT được lưu trong localStorage với key "aurapc_token" (user) hoặc "aurapc_admin_token" (admin).'),
    bullet('**Auth Interceptor:** Mọi HTTP request từ Angular tự động đính kèm header "Authorization: Bearer <token>" thông qua functional interceptor.'),
    bullet('**Bảo mật:** JWT_SECRET là biến môi trường bắt buộc — server sẽ crash khi khởi động nếu không được cấu hình, tránh việc sử dụng fallback secret không an toàn.'),
    spacer(),

    h3('2.1.6. Node.js và Express.js'),
    p('**Node.js** là môi trường runtime JavaScript mã nguồn mở, đa nền tảng, được xây dựng trên engine V8 của Google Chrome. Node.js cho phép thực thi JavaScript phía server, sử dụng mô hình I/O không chặn (non-blocking I/O) và kiến trúc hướng sự kiện (event-driven), phù hợp cho các ứng dụng real-time và xử lý đồng thời nhiều kết nối.', { indent: true }),
    p('**Express.js** (phiên bản 5.2 trong AuraPC) là framework web tối giản, linh hoạt cho Node.js, cung cấp bộ tính năng mạnh mẽ để xây dựng web application và REST API. Express.js là framework phổ biến nhất trong hệ sinh thái Node.js.', { indent: true }),
    p('Vai trò trong AuraPC:', { indent: true }),
    bullet('**API Server:** Express.js xử lý toàn bộ logic backend với hơn 80 API endpoints, tổ chức theo Router modules.'),
    bullet('**Middleware Pipeline:** Chuỗi middleware xử lý: CORS → JSON parsing → Static files → Route handlers → Error handler.'),
    bullet('**Authentication Middleware:** requireAuth, optionalAuth, requireAdmin, requireUserOrAdmin — kiểm soát truy cập API.'),
    bullet('**Socket.IO Integration:** Kết hợp HTTP server với Socket.IO để cung cấp real-time communication cho thông báo đơn hàng và hỗ trợ trực tuyến.'),
    bullet('**File Upload:** Sử dụng multer middleware để xử lý upload ảnh đại diện và ảnh bài đăng AuraHub.'),
    spacer(),

    h3('2.1.7. Three.js và khái niệm 3D trên Web'),
    p('**Three.js** là thư viện JavaScript mã nguồn mở để tạo và hiển thị đồ họa 3D trên trình duyệt web sử dụng WebGL. Three.js trừu tượng hóa các API WebGL phức tạp, cung cấp interface dễ sử dụng để tạo scene, camera, lighting, geometry, material và animation 3D.', { indent: true }),
    p('Trong AuraPC, Three.js được sử dụng cho hai mục đích chính:', { indent: true }),
    bullet('**Trang chủ 3D Canvas:** Component ThreeCanvasComponent render một scene 3D tương tác trên homepage, bao gồm: mô hình 3D PC, hiệu ứng ánh sáng, hotspot interaction (click vào các điểm trên mô hình để xem thông tin), camera controls cho phép người dùng xoay và zoom.'),
    bullet('**Product 3D Viewer:** Sử dụng Google Model Viewer (<model-viewer>) trên trang chi tiết sản phẩm để hiển thị mô hình 3D của sản phẩm (nếu có), cho phép người dùng xoay, phóng to và xem sản phẩm từ mọi góc độ.'),
    p('Các khái niệm 3D cốt lõi được áp dụng:', { indent: true }),
    bullet('**Scene:** Không gian 3D chứa tất cả các đối tượng, ánh sáng và camera.'),
    bullet('**Camera:** Góc nhìn của người dùng vào scene (PerspectiveCamera cho góc nhìn phối cảnh).'),
    bullet('**Renderer:** WebGLRenderer chuyển đổi scene 3D thành hình ảnh 2D hiển thị trên canvas.'),
    bullet('**Lighting:** Ambient Light (ánh sáng môi trường), Directional Light (ánh sáng định hướng), Point Light (ánh sáng điểm).'),
    bullet('**GLB/GLTF:** Định dạng file 3D tiêu chuẩn web, được sử dụng để tải mô hình PC trong AuraPC.'),
    spacer(),

    h3('2.1.8. Angular Framework'),
    p('**Angular** là framework phát triển ứng dụng web mã nguồn mở do Google phát triển và bảo trì. Angular sử dụng TypeScript làm ngôn ngữ chính và áp dụng kiến trúc component-based, cho phép xây dựng Single Page Application (SPA) với hiệu suất cao và khả năng bảo trì tốt.', { indent: true }),
    p('AuraPC sử dụng **Angular 21** — phiên bản mới nhất với các đặc điểm nổi bật:', { indent: true }),
    bullet('**Standalone Components:** Tất cả component trong AuraPC đều là standalone (không sử dụng NgModules truyền thống), giảm boilerplate code và cải thiện tree-shaking.'),
    bullet('**Signal-based State Management:** Sử dụng Angular Signals (signal(), computed(), effect()) để quản lý trạng thái ứng dụng một cách reactive và hiệu quả hơn RxJS cho nhiều use case.'),
    bullet('**Lazy Loading:** Các route được lazy-load để tối ưu bundle size — chỉ tải code khi người dùng truy cập trang tương ứng.'),
    bullet('**Functional Interceptors:** Auth interceptor sử dụng functional API mới thay vì class-based interceptor truyền thống.'),
    bullet('**Dependency Injection (DI):** Hệ thống DI mạnh mẽ cho phép inject service vào component một cách linh hoạt.'),
    bullet('**Reactive Forms:** Xử lý form phức tạp (đặt hàng, quản lý sản phẩm) với validation theo thời gian thực.'),
    p('Kiến trúc Angular trong AuraPC:', { indent: true }),
    bullet('**2 ứng dụng:** my-client (khách hàng, port 4200) và my-admin (quản trị, port 4201).'),
    bullet('**19 page components** và **9 shared components** cho ứng dụng khách hàng.'),
    bullet('**11 page directories** và **2 shared components** cho ứng dụng admin.'),
    bullet('**12 injectable services** xử lý logic nghiệp vụ, gọi API và quản lý trạng thái.'),
    spacer(),

    h3('2.1.9. Cơ sở dữ liệu NoSQL'),
    p('**NoSQL (Not Only SQL)** là thuật ngữ chỉ các hệ quản trị cơ sở dữ liệu không sử dụng mô hình bảng quan hệ truyền thống (relational). NoSQL được thiết kế để xử lý lượng dữ liệu lớn, phi cấu trúc hoặc bán cấu trúc, với khả năng mở rộng ngang (horizontal scaling) linh hoạt.', { indent: true }),
    p('Các loại NoSQL database phổ biến:', { indent: true }),
    bullet('**Document Database:** Lưu trữ dữ liệu dưới dạng document (thường là JSON/BSON). Ví dụ: MongoDB, CouchDB.'),
    bullet('**Key-Value Store:** Lưu trữ cặp key-value đơn giản. Ví dụ: Redis, DynamoDB.'),
    bullet('**Column-family Store:** Lưu trữ dữ liệu theo cột. Ví dụ: Cassandra, HBase.'),
    bullet('**Graph Database:** Lưu trữ dữ liệu dưới dạng đồ thị (nodes và edges). Ví dụ: Neo4j.'),
    p('AuraPC sử dụng **MongoDB** — Document Database phổ biến nhất, vì các lý do:', { indent: true }),
    bullet('**Schema linh hoạt:** Sản phẩm PC gaming có nhiều đặc tính kỹ thuật khác nhau tùy loại (CPU có clock speed, GPU có VRAM, RAM có dung lượng) — document model cho phép mỗi sản phẩm có specs khác nhau mà không cần alter table.'),
    bullet('**JSON-native:** Dữ liệu lưu trữ dưới dạng BSON (Binary JSON), tương thích tự nhiên với JavaScript/Node.js backend và Angular frontend.'),
    bullet('**Hiệu suất đọc cao:** Phù hợp cho ứng dụng TMĐT với tỷ lệ đọc/ghi cao (người dùng browse sản phẩm nhiều hơn đặt hàng).'),
    bullet('**Embedded Documents:** Cho phép nhúng sub-document (địa chỉ trong User, items trong Order) giảm số lượng JOIN queries.'),
    spacer(),

    // ===== 2.2 =====
    h2('2.2. Công cụ sử dụng'),

    h3('2.2.1. MongoDB và Mongoose'),
    p('**MongoDB** là hệ quản trị cơ sở dữ liệu NoSQL document-oriented, mã nguồn mở, được phát triển bởi MongoDB Inc. MongoDB lưu trữ dữ liệu dưới dạng BSON documents trong các collections, cung cấp khả năng truy vấn linh hoạt, indexing mạnh mẽ và horizontal scaling qua sharding.', { indent: true }),
    p('**Mongoose** (phiên bản 9.1) là ODM (Object Document Mapper) cho Node.js và MongoDB, cung cấp lớp trừu tượng để định nghĩa schema, validation, middleware (pre/post hooks) và query building. Mongoose giúp làm việc với MongoDB dễ dàng hơn bằng cách áp đặt cấu trúc cho các document.', { indent: true }),
    p('Trong AuraPC, MongoDB Atlas (cloud) được sử dụng cho production, với **20 Mongoose models** định nghĩa đầy đủ schema cho: User, Admin, Product, Category, Order, Cart, ProductReview, Blog, Builder, Post, HubComment, Share, Promotion, PromotionUsage, Otp, UserNotification, AdminNotification, Faq, SupportConversation, SupportMessage.', { indent: true }),
    spacer(),

    h3('2.2.2. Visual Studio Code'),
    p('**Visual Studio Code (VS Code)** là trình biên tập mã nguồn miễn phí, mã nguồn mở do Microsoft phát triển. VS Code hỗ trợ đa ngôn ngữ lập trình, tích hợp terminal, debugger, Git và hệ sinh thái extensions phong phú. VS Code là IDE chính được sử dụng trong toàn bộ quá trình phát triển AuraPC, với các extensions quan trọng: Angular Language Service, ESLint, Prettier, MongoDB for VS Code, Thunder Client.', { indent: true }),
    spacer(),

    h3('2.2.3. Git và GitHub'),
    p('**Git** là hệ thống quản lý phiên bản phân tán (Distributed Version Control System) mã nguồn mở, cho phép theo dõi lịch sử thay đổi mã nguồn, quản lý nhánh (branching) và hợp nhất (merging) code.', { indent: true }),
    p('**GitHub** là nền tảng hosting dựa trên Git, cung cấp giao diện web để quản lý repository, review code, theo dõi issues và tự động hóa quy trình CI/CD.', { indent: true }),
    p('AuraPC sử dụng Git/GitHub cho:', { indent: true }),
    bullet('**Version Control:** Toàn bộ mã nguồn (frontend, backend, shared library) được quản lý trong một monorepo trên GitHub.'),
    bullet('**Branching Strategy:** Sử dụng feature branches (feature/*, fix/*) merge vào main branch.'),
    bullet('**Auto-deployment:** GitHub tích hợp với Vercel (frontend) và Render (backend) để tự động deploy khi push lên main.'),
    spacer(),

    h3('2.2.4. Draw.io'),
    p('**Draw.io (diagrams.net)** là công cụ vẽ sơ đồ trực tuyến miễn phí, hỗ trợ nhiều loại diagram: flowchart, UML, DFD, BPMN, ERD, wireframe. Trong đồ án, Draw.io được sử dụng để vẽ các sơ đồ phân tích và thiết kế hệ thống bao gồm: Context Diagram (DFD Level 0), DFD Level 1, sơ đồ Use Case, sơ đồ BPMN và sitemap.', { indent: true }),
    spacer(),

    h3('2.2.5. Figma'),
    p('**Figma** là công cụ thiết kế giao diện UI/UX dựa trên nền tảng web, hỗ trợ cộng tác thời gian thực. Figma được sử dụng trong AuraPC để:', { indent: true }),
    bullet('Thiết kế mockup và wireframe cho các trang giao diện.'),
    bullet('Xây dựng Design System: color palette, typography, spacing, component library.'),
    bullet('Prototyping: Tạo prototype tương tác để kiểm thử luồng người dùng trước khi code.'),
    bullet('Handoff: Cung cấp thông số thiết kế (CSS values, assets) cho developer.'),
    spacer(),

    h3('2.2.7. Postman (Công cụ kiểm thử API)'),
    p('**Postman** là công cụ phát triển và kiểm thử API phổ biến, cho phép gửi HTTP requests, kiểm tra response, tạo test suites và tài liệu API tự động. Trong AuraPC, Postman được sử dụng để:', { indent: true }),
    bullet('Kiểm thử tất cả 80+ API endpoints trong quá trình phát triển.'),
    bullet('Tạo collections theo nhóm chức năng: Auth, Products, Orders, Payment, Admin.'),
    bullet('Test luồng thanh toán MoMo/ZaloPay (sandbox mode).'),
    bullet('Debug JWT authentication và authorization flow.'),
    spacer(),

    h3('2.2.8. Blender'),
    p('**Blender** là phần mềm mã nguồn mở dùng để tạo nội dung 3D, bao gồm: modeling, sculpting, texturing, animation, rendering và compositing. Trong AuraPC, Blender được sử dụng để:', { indent: true }),
    bullet('Tạo mô hình 3D PC gaming (case, linh kiện) cho trang chủ Three.js canvas.'),
    bullet('Export mô hình sang định dạng GLB/GLTF — định dạng 3D tối ưu cho web.'),
    bullet('Tối ưu hóa polygon count và texture resolution để đảm bảo hiệu suất trên trình duyệt.'),
    bullet('Tạo hình ảnh render cho AuraVisual trong PC Builder.'),
    spacer(),

    h3('2.2.9. N8N'),
    p('**N8N** là nền tảng tự động hóa workflow mã nguồn mở, cho phép kết nối và tự động hóa các dịch vụ khác nhau thông qua giao diện kéo thả trực quan. N8N hỗ trợ hơn 400 tích hợp (integrations) và cho phép tạo workflow tùy chỉnh bằng JavaScript/Python.', { indent: true }),
    p('Trong AuraPC, N8N được sử dụng cho:', { indent: true }),
    bullet('**Tự động hóa quy trình:** Trigger workflow khi có đơn hàng mới, tự động gửi email xác nhận.'),
    bullet('**Data Pipeline:** Thu thập và đồng bộ dữ liệu sản phẩm từ các nguồn khác nhau.'),
    bullet('**Webhook Integration:** Nhận callback từ cổng thanh toán và trigger các hành động tương ứng.'),
    bullet('**Monitoring:** Giám sát trạng thái server và gửi cảnh báo khi có sự cố.'),
  ];
};
