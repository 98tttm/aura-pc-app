const { h1, h2, h3, h4, p, bullet, numberedItem, spacer, pageBreak, makeTable, figureCaption } = require('./report-helpers');

module.exports = function chapter3() {
  return [
    h1('CHƯƠNG 3: PHÂN TÍCH VÀ THIẾT KẾ HỆ THỐNG'),
    pageBreak(),

    // ===== 3.1 SITEMAP =====
    h2('3.1. Sitemap'),
    p('Sitemap mô tả cấu trúc tổ chức các trang của website, giúp hình dung tổng quan về hệ thống điều hướng và mối quan hệ giữa các trang. AuraPC được tổ chức thành 3 sitemap chính: trang chủ, khu vực người dùng và khu vực quản trị viên.', { indent: true }),

    h3('3.1.1. Sitemap trang chủ'),
    p('Trang chủ AuraPC là điểm bắt đầu của toàn bộ website, từ đây người dùng có thể truy cập tất cả các khu vực chức năng:', { indent: true }),
    makeTable(
      [2400, 3200, 3760],
      ['Trang', 'Đường dẫn (Route)', 'Mô tả'],
      [
        ['Trang Chủ', '/', 'Landing page với 3D canvas, sản phẩm nổi bật, intro animation'],
        ['Sản phẩm', '/san-pham', 'Danh sách sản phẩm với bộ lọc, tìm kiếm, phân trang'],
        ['Chi tiết SP', '/san-pham/:slug', 'Thông tin chi tiết, đánh giá, thông số kỹ thuật, 3D viewer'],
        ['Giỏ hàng', '/cart', 'Quản lý giỏ hàng, mã khuyến mãi'],
        ['Đặt hàng', '/checkout', 'Chọn địa chỉ, phương thức thanh toán, xác nhận'],
        ['Blog', '/blog', 'Danh sách bài viết tin tức công nghệ'],
        ['Chi tiết Blog', '/blog/:slug', 'Nội dung bài viết đầy đủ'],
        ['Aura Builder', '/aura-builder', 'Công cụ lắp ráp cấu hình PC'],
        ['AuraHub', '/aura-hub', 'Cộng đồng mạng xã hội gaming'],
        ['Hỗ trợ', '/ho-tro', 'FAQ và hỗ trợ trực tuyến'],
        ['Tra cứu đơn', '/tra-cuu-don-hang', 'Tra cứu đơn hàng (không cần đăng nhập)'],
        ['Về AuraPC', '/ve-aurapc', 'Thông tin thương hiệu'],
      ]
    ),
    spacer(),

    h3('3.1.2. Sitemap người dùng'),
    p('Khu vực dành cho người dùng đã đăng nhập, quản lý tài khoản cá nhân và các hoạt động mua sắm:', { indent: true }),
    makeTable(
      [2400, 3200, 3760],
      ['Trang / Chức năng', 'Đường dẫn', 'Mô tả'],
      [
        ['Tài khoản', '/tai-khoan', 'Dashboard cá nhân: hồ sơ, địa chỉ, lịch sử đơn hàng'],
        ['Hồ sơ cá nhân', '/tai-khoan (tab)', 'Cập nhật tên, ngày sinh, giới tính, avatar, email'],
        ['Quản lý địa chỉ', '/tai-khoan (tab)', 'Thêm/sửa/xóa địa chỉ giao hàng, đặt mặc định'],
        ['Lịch sử đơn hàng', '/tai-khoan (tab)', 'Danh sách đơn hàng với bộ lọc trạng thái'],
        ['Chi tiết đơn hàng', '/tai-khoan (tab)', 'Thông tin đơn, yêu cầu hủy/trả, xác nhận nhận hàng'],
        ['Thông báo', '/tai-khoan (tab)', 'Thông báo trạng thái đơn hàng real-time'],
        ['Thanh toán MoMo', '/checkout-momo-return', 'Xác nhận kết quả thanh toán MoMo'],
        ['Thanh toán ZaloPay', '/checkout-zalopay-return', 'Xác nhận kết quả thanh toán ZaloPay'],
        ['Thanh toán ATM/QR', '/checkout-atm-payment', 'Hướng dẫn chuyển khoản ATM'],
        ['Đặt hàng thành công', '/checkout-success', 'Xác nhận đơn hàng thành công'],
      ]
    ),
    spacer(),

    h3('3.1.3. Sitemap quản trị viên'),
    p('Khu vực quản trị viên (Admin Panel) tại port 4201, yêu cầu đăng nhập admin:', { indent: true }),
    makeTable(
      [2400, 3200, 3760],
      ['Trang', 'Đường dẫn', 'Mô tả'],
      [
        ['Đăng nhập', '/login', 'Đăng nhập bằng email/mật khẩu admin'],
        ['Dashboard', '/', 'Thống kê KPI, biểu đồ doanh thu, đơn hàng gần đây'],
        ['Quản lý sản phẩm', '/products', 'CRUD sản phẩm, thống kê tồn kho'],
        ['Thêm/Sửa SP', '/products/new, /products/:id/edit', 'Form thêm mới hoặc chỉnh sửa sản phẩm'],
        ['Quản lý danh mục', '/categories', 'CRUD danh mục sản phẩm'],
        ['Quản lý đơn hàng', '/orders', 'Danh sách đơn, bộ lọc trạng thái, tìm kiếm'],
        ['Chi tiết đơn hàng', '/orders/:orderNumber', 'Cập nhật trạng thái, duyệt hủy/trả'],
        ['Quản lý khách hàng', '/users', 'Danh sách users, phân khúc (VIP, Regular, New)'],
        ['Chi tiết khách hàng', '/users/:id', 'Hồ sơ chi tiết, lịch sử đơn hàng'],
        ['Quản lý blog', '/blogs', 'CRUD bài viết, nháp/xuất bản'],
        ['Quản lý AuraHub', '/hub', 'Kiểm duyệt bài đăng cộng đồng (duyệt/từ chối/xóa)'],
        ['Hỗ trợ khách hàng', '/support', 'Chat real-time với khách hàng'],
        ['Quản lý khuyến mãi', '/promotions', 'CRUD mã voucher giảm giá'],
      ]
    ),
    spacer(),

    // ===== 3.2 USE CASE =====
    h2('3.2. Sơ đồ Use Case'),

    h3('3.2.1. Sơ đồ Use Case tổng quát'),
    p('Hệ thống AuraPC có 2 tác nhân chính:', { indent: true }),
    bullet('**Khách hàng (Customer):** Bao gồm khách vãng lai (Guest) và người dùng đã đăng ký (Registered User). Guest có thể duyệt sản phẩm, thêm giỏ hàng và tra cứu đơn hàng. Registered User có đầy đủ chức năng: mua hàng, đánh giá, tham gia cộng đồng, chat hỗ trợ.'),
    bullet('**Quản trị viên (Administrator):** Quản lý toàn bộ hệ thống thông qua Admin Panel riêng biệt.'),
    p('Các Use Case tổng quát của hệ thống bao gồm:', { indent: true }),

    makeTable(
      [600, 3000, 2400, 3360],
      ['STT', 'Use Case', 'Tác nhân', 'Mô tả ngắn'],
      [
        ['1', 'Đăng ký / Đăng nhập (OTP)', 'Khách hàng', 'Xác thực bằng SĐT + mã OTP 6 số'],
        ['2', 'Duyệt sản phẩm', 'Khách hàng, Guest', 'Xem danh sách, tìm kiếm, lọc, xem chi tiết'],
        ['3', 'Quản lý giỏ hàng', 'Khách hàng, Guest', 'Thêm, xóa, cập nhật số lượng SP trong giỏ'],
        ['4', 'Đặt hàng và thanh toán', 'Khách hàng', 'Chọn địa chỉ, PTTT, tạo đơn hàng'],
        ['5', 'Theo dõi đơn hàng', 'Khách hàng, Guest', 'Tra cứu trạng thái đơn bằng mã đơn'],
        ['6', 'Yêu cầu hủy/trả hàng', 'Khách hàng', 'Gửi yêu cầu hủy hoặc trả sản phẩm'],
        ['7', 'Đánh giá sản phẩm', 'Khách hàng', 'Viết review (1-5 sao) hoặc bình luận'],
        ['8', 'Sử dụng PC Builder', 'Khách hàng, Guest', 'Lắp ráp cấu hình PC, xuất PDF, chia sẻ'],
        ['9', 'Tham gia AuraHub', 'Khách hàng', 'Đăng bài, bình luận, thích, repost, bình chọn'],
        ['10', 'Chat với AI (AruBot)', 'Khách hàng, Guest', 'Nhận tư vấn sản phẩm từ AI chatbot'],
        ['11', 'Liên hệ hỗ trợ', 'Khách hàng', 'Chat real-time với admin hỗ trợ'],
        ['12', 'Quản lý tài khoản', 'Khách hàng', 'Cập nhật hồ sơ, địa chỉ, avatar'],
        ['13', 'Đăng nhập Admin', 'Quản trị viên', 'Đăng nhập bằng email/mật khẩu'],
        ['14', 'Quản lý sản phẩm', 'Quản trị viên', 'CRUD sản phẩm, toggle active, quản lý stock'],
        ['15', 'Quản lý đơn hàng', 'Quản trị viên', 'Cập nhật trạng thái, duyệt hủy/trả'],
        ['16', 'Quản lý khách hàng', 'Quản trị viên', 'Xem danh sách, chi tiết, phân khúc, ban user'],
        ['17', 'Quản lý bài viết', 'Quản trị viên', 'CRUD blog, kiểm duyệt bài Hub'],
        ['18', 'Quản lý khuyến mãi', 'Quản trị viên', 'CRUD mã voucher, theo dõi sử dụng'],
        ['19', 'Hỗ trợ khách hàng', 'Quản trị viên', 'Trả lời chat hỗ trợ real-time'],
        ['20', 'Xem Dashboard', 'Quản trị viên', 'Thống kê doanh thu, biểu đồ, top SP'],
      ]
    ),
    spacer(),

    h3('3.2.2. Các sơ đồ Use Case của người dùng'),
    p('Dưới đây là mô tả chi tiết các Use Case chính của người dùng:', { indent: true }),

    h4('UC01: Đăng ký và Đăng nhập'),
    p('**Tác nhân:** Khách hàng', { indent: true }),
    p('**Mô tả:** Người dùng nhập số điện thoại, hệ thống gửi mã OTP 6 số. Người dùng nhập OTP để xác thực. Nếu SĐT chưa tồn tại trong hệ thống, tài khoản mới được tạo tự động. Sau khi xác thực thành công, server trả về JWT token (7 ngày) và thông tin user.', { indent: true }),
    p('**Luồng chính:**', { indent: true }),
    numberedItem('Người dùng truy cập form đăng nhập và nhập số điện thoại (định dạng 0xxxxxxxxx hoặc 84xxxxxxxxx).'),
    numberedItem('Hệ thống gửi yêu cầu POST /api/auth/request-otp, server tạo OTP 6 số và lưu vào collection Otp (TTL 5 phút).'),
    numberedItem('Người dùng nhận OTP (trong production qua SMS, trong development hiển thị trên response) và nhập vào form.'),
    numberedItem('Hệ thống gửi POST /api/auth/verify-otp, server xác thực OTP, tạo/cập nhật user và trả JWT token.'),
    numberedItem('Frontend lưu token vào localStorage và điều hướng người dùng về trang trước đó.'),
    p('**Luồng ngoại lệ:** OTP hết hạn (5 phút), OTP sai, SĐT không hợp lệ.', { indent: true }),
    spacer(),

    h4('UC02: Duyệt và Tìm kiếm sản phẩm'),
    p('**Tác nhân:** Khách hàng, Guest', { indent: true }),
    p('**Mô tả:** Người dùng có thể duyệt sản phẩm theo danh mục, tìm kiếm theo từ khóa, lọc theo nhiều tiêu chí (brand, giá, specs) và sắp xếp theo giá/tên. Trang chi tiết sản phẩm hiển thị thông tin đầy đủ: hình ảnh, thông số kỹ thuật, đánh giá, bình luận và 3D viewer.', { indent: true }),
    spacer(),

    h4('UC03: Quản lý giỏ hàng'),
    p('**Tác nhân:** Khách hàng, Guest', { indent: true }),
    p('**Mô tả:** Guest sử dụng giỏ hàng localStorage. Registered User sử dụng giỏ hàng đồng bộ với server (Cart model). Khi đăng nhập, giỏ hàng local được sync lên server (merge, lấy quantity lớn hơn). Hệ thống kiểm tra tồn kho trước khi thêm sản phẩm.', { indent: true }),
    spacer(),

    h4('UC04: Đặt hàng và Thanh toán'),
    p('**Tác nhân:** Khách hàng', { indent: true }),
    p('**Mô tả:** Quy trình đặt hàng gồm các bước: chọn sản phẩm → chọn địa chỉ giao hàng → áp dụng mã khuyến mãi → chọn phương thức thanh toán → xác nhận đơn hàng. Hệ thống hỗ trợ 5 phương thức thanh toán: COD (với OTP xác nhận), MoMo, ZaloPay, chuyển khoản ATM và QR code.', { indent: true }),
    spacer(),

    h4('UC05-UC12: Các Use Case khác'),
    p('Các Use Case còn lại bao gồm: Theo dõi đơn hàng (UC05), Yêu cầu hủy/trả (UC06), Đánh giá sản phẩm (UC07), PC Builder (UC08), AuraHub (UC09), AI Chatbot (UC10), Hỗ trợ trực tuyến (UC11), Quản lý tài khoản (UC12) — mỗi Use Case đều có luồng chính, luồng ngoại lệ và điều kiện tiên quyết tương ứng.', { indent: true }),
    spacer(),

    h3('3.2.3. Các sơ đồ Use Case của quản trị viên'),
    p('Quản trị viên truy cập Admin Panel (port 4201) với các Use Case chính:', { indent: true }),
    bullet('**UC13 — Đăng nhập Admin:** Xác thực bằng email/password (bcrypt hash), nhận JWT với claim isAdmin: true.'),
    bullet('**UC14 — Quản lý sản phẩm:** CRUD sản phẩm, toggle active/inactive, upload ảnh, quản lý tồn kho (stock).'),
    bullet('**UC15 — Quản lý đơn hàng:** Cập nhật trạng thái (pending → confirmed → processing → shipped → delivered), xác nhận thanh toán COD, duyệt/từ chối yêu cầu hủy hoặc trả hàng.'),
    bullet('**UC16 — Quản lý khách hàng:** Xem danh sách users với phân khúc (VIP: 10+ đơn, Regular: 3+, New: 1, Inactive: 0), xem chi tiết, ban user.'),
    bullet('**UC17 — Quản lý bài viết:** CRUD blog posts (nháp/xuất bản), kiểm duyệt bài đăng AuraHub (duyệt/từ chối/xóa).'),
    bullet('**UC18 — Quản lý khuyến mãi:** CRUD mã voucher với: phần trăm giảm, giá trị tối đa, đơn tối thiểu, giới hạn sử dụng, ngày bắt đầu/kết thúc.'),
    bullet('**UC19 — Hỗ trợ khách hàng:** Trả lời chat real-time qua Socket.IO, xem danh sách hội thoại, lưu trữ cuộc trò chuyện.'),
    bullet('**UC20 — Xem Dashboard:** Thống kê tổng quan (doanh thu, đơn hàng, khách hàng), biểu đồ xu hướng (7-12 tháng), top sản phẩm bán chạy.'),
    spacer(),

    // ===== 3.3 BPMN =====
    h2('3.3. Sơ đồ BPMN'),
    p('BPMN (Business Process Model and Notation) là chuẩn quốc tế để mô hình hóa quy trình nghiệp vụ. Dưới đây là mô tả chi tiết các quy trình BPMN chính trong hệ thống AuraPC.', { indent: true }),

    h3('3.3.1. Sơ đồ BPMN của người dùng'),

    h4('3.3.2.1. Đăng ký và đăng nhập'),
    p('**Pool:** Người dùng | Hệ thống AuraPC', { indent: true }),
    p('**Quy trình:**', { indent: true }),
    numberedItem('(Start) Người dùng mở trang đăng nhập.'),
    numberedItem('Người dùng nhập số điện thoại → Hệ thống validate format SĐT.'),
    numberedItem('(Gateway XOR) SĐT hợp lệ? → Không: Hiển thị lỗi, quay lại bước 2.'),
    numberedItem('Hệ thống tạo OTP 6 số → Lưu vào MongoDB (TTL 5 phút) → Gửi OTP cho người dùng.'),
    numberedItem('Người dùng nhập mã OTP → Hệ thống xác thực.'),
    numberedItem('(Gateway XOR) OTP đúng? → Không: Hiển thị lỗi, cho phép nhập lại hoặc gửi lại OTP.'),
    numberedItem('(Gateway XOR) SĐT đã tồn tại? → Không: Tạo tài khoản mới. Có: Cập nhật lastLogin.'),
    numberedItem('Hệ thống tạo JWT token (7 ngày) → Trả về user info + token.'),
    numberedItem('Frontend lưu token vào localStorage → Sync giỏ hàng local lên server.'),
    numberedItem('(End) Điều hướng người dùng về trang trước đó.'),
    spacer(),

    h4('3.3.2.2. Đặt hàng và thanh toán'),
    p('**Pool:** Người dùng | Hệ thống AuraPC | Cổng thanh toán (MoMo/ZaloPay)', { indent: true }),
    p('**Quy trình:**', { indent: true }),
    numberedItem('(Start) Người dùng nhấn "Thanh toán" từ giỏ hàng.'),
    numberedItem('Hệ thống hiển thị form checkout: địa chỉ giao hàng, phương thức thanh toán.'),
    numberedItem('Người dùng chọn/nhập địa chỉ → Nhập mã khuyến mãi (tùy chọn).'),
    numberedItem('(Gateway XOR) Mã KM hợp lệ? → Hệ thống validate: ngày hiệu lực, giới hạn sử dụng, đơn tối thiểu.'),
    numberedItem('Người dùng chọn phương thức thanh toán.'),
    numberedItem('(Gateway XOR) Phương thức thanh toán:'),
    bullet('**COD:** Hiển thị dialog xác nhận OTP → Xác thực → Tạo đơn hàng (status: pending, isPaid: false).', 1),
    bullet('**MoMo:** Hệ thống gọi MoMo API tạo giao dịch → Redirect người dùng sang MoMo → MoMo xử lý → IPN callback → Tạo đơn hàng (isPaid: true).', 1),
    bullet('**ZaloPay:** Tương tự MoMo — gọi ZaloPay API → Redirect → Callback → Tạo đơn.', 1),
    bullet('**ATM/QR:** Hiển thị thông tin chuyển khoản → Người dùng tự chuyển → Admin xác nhận thủ công.', 1),
    numberedItem('Hệ thống kiểm tra tồn kho (stock) → Kiểm tra giá server-side → Kiểm tra trùng lặp đơn (15 phút).'),
    numberedItem('Tạo đơn hàng (Order model) → Giảm stock sản phẩm → Xóa sản phẩm khỏi giỏ hàng.'),
    numberedItem('(Parallel Gateway) Gửi thông báo: AdminNotification + UserNotification + Email hóa đơn (tùy chọn).'),
    numberedItem('(End) Hiển thị trang xác nhận đơn hàng thành công.'),
    spacer(),

    h4('3.3.2.3. Theo dõi đơn hàng'),
    p('**Quy trình:**', { indent: true }),
    numberedItem('(Start) Người dùng truy cập trang tra cứu đơn hàng hoặc tab lịch sử đơn hàng trong tài khoản.'),
    numberedItem('Nhập mã đơn hàng (nếu guest) hoặc xem danh sách (nếu đã đăng nhập).'),
    numberedItem('Hệ thống truy vấn đơn hàng → Hiển thị trạng thái, chi tiết sản phẩm, thông tin giao hàng.'),
    numberedItem('(Event) Socket.IO đẩy cập nhật real-time khi admin thay đổi trạng thái đơn hàng.'),
    numberedItem('(End) Người dùng xem được trạng thái đơn hàng: pending → confirmed → processing → shipped → delivered.'),
    spacer(),

    h4('3.3.2.4. Hủy/Hoàn trả đơn hàng'),
    p('**Quy trình:**', { indent: true }),
    numberedItem('(Start) Người dùng xem chi tiết đơn hàng → Nhấn "Yêu cầu hủy" hoặc "Yêu cầu trả hàng".'),
    numberedItem('(Gateway XOR) Đơn hàng có đủ điều kiện? → Hủy: chỉ khi status là pending/confirmed/processing. Trả: chỉ khi status là shipped/delivered.'),
    numberedItem('Người dùng nhập lý do → Hệ thống tạo cancelRequest/returnRequest (status: pending).'),
    numberedItem('Hệ thống gửi AdminNotification thông báo yêu cầu mới.'),
    numberedItem('Admin xem yêu cầu → Duyệt hoặc từ chối (kèm ghi chú).'),
    numberedItem('Hệ thống cập nhật request status → Gửi UserNotification cho khách hàng.'),
    numberedItem('(End) Nếu duyệt hủy: đơn chuyển sang cancelled. Nếu duyệt trả: xử lý hoàn trả.'),
    spacer(),

    h4('3.3.2.5. Đánh giá sản phẩm'),
    p('**Quy trình:**', { indent: true }),
    numberedItem('(Start) Người dùng truy cập trang chi tiết sản phẩm → Tab đánh giá.'),
    numberedItem('(Gateway XOR) Loại tương tác: Review (đánh giá có sao) hay Comment (bình luận không sao)?'),
    numberedItem('Review: Hệ thống kiểm tra người dùng đã mua và nhận hàng (order delivered) → Chọn số sao (1-5) → Viết nội dung → Đính kèm ảnh (tùy chọn).'),
    numberedItem('Comment: Mọi user đã đăng nhập đều có thể bình luận → Viết nội dung.'),
    numberedItem('Người dùng có thể reply (phản hồi) review/comment khác → Tạo nested thread.'),
    numberedItem('(End) Đánh giá/bình luận được hiển thị trên trang sản phẩm.'),
    spacer(),

    h4('3.3.2.6. Quản lý tài khoản'),
    p('**Quy trình:**', { indent: true }),
    numberedItem('(Start) Người dùng truy cập trang Tài khoản (/tai-khoan).'),
    numberedItem('Cập nhật hồ sơ: Họ tên, ngày sinh, giới tính, email → POST /api/auth/profile.'),
    numberedItem('Upload avatar: Chọn ảnh → POST /api/auth/avatar (multer xử lý, lưu /uploads/avatar-*).'),
    numberedItem('Quản lý địa chỉ: Thêm mới → Sửa → Xóa → Đặt mặc định (tự động bỏ default cũ).'),
    numberedItem('Follow/Unfollow người dùng khác trên AuraHub.'),
    numberedItem('(End) Thông tin được cập nhật và đồng bộ real-time trên giao diện.'),
    spacer(),

    h3('3.3.2. Sơ đồ BPMN của quản trị viên'),

    h4('3.2.2.1. Đăng nhập'),
    p('**Quy trình:**', { indent: true }),
    numberedItem('(Start) Admin truy cập trang đăng nhập Admin Panel (/login).'),
    numberedItem('Nhập email và mật khẩu → POST /api/admin/auth/login.'),
    numberedItem('Server so sánh password bằng bcrypt.compare() → Tạo JWT (adminId, isAdmin: true).'),
    numberedItem('Frontend lưu token → Kết nối Socket.IO (room: admin) → Redirect về Dashboard.'),
    numberedItem('(End) Admin truy cập được các chức năng quản trị.'),
    spacer(),

    h4('3.2.2.2. Quản lý sản phẩm'),
    p('**Quy trình:**', { indent: true }),
    numberedItem('(Start) Admin truy cập trang Sản phẩm (/products).'),
    numberedItem('Xem danh sách sản phẩm → Tìm kiếm, lọc theo danh mục/trạng thái/tồn kho.'),
    numberedItem('(Gateway XOR) Thao tác: Thêm mới | Sửa | Xóa | Toggle Active.'),
    numberedItem('Thêm/Sửa: Form nhập thông tin (tên, slug, giá, mô tả, ảnh, specs, danh mục, stock).'),
    numberedItem('Xóa: Hiển thị ConfirmDialog → Xác nhận → DELETE /api/admin/products/:id.'),
    numberedItem('Toggle Active: PATCH /api/admin/products/:id/toggle-active → Ẩn/hiện sản phẩm.'),
    numberedItem('(End) Danh sách sản phẩm được cập nhật.'),
    spacer(),

    h4('3.2.2.3. Quản lý đơn hàng'),
    p('**Quy trình:**', { indent: true }),
    numberedItem('(Start) Admin truy cập trang Đơn hàng (/orders).'),
    numberedItem('Xem danh sách đơn → Lọc theo trạng thái (pending/confirmed/processing/shipped/delivered/cancelled).'),
    numberedItem('Xem chi tiết đơn → Stepper hiển thị tiến trình đơn hàng.'),
    numberedItem('Cập nhật trạng thái: pending → confirmed → processing → shipped → delivered.'),
    numberedItem('(Parallel) Gửi UserNotification + AdminNotification + Socket.IO event (order:updated).'),
    numberedItem('Xử lý yêu cầu hủy/trả: Duyệt hoặc từ chối (kèm ghi chú) → Gửi thông báo cho khách.'),
    numberedItem('Xác nhận thanh toán COD: Đánh dấu đơn hàng là đã thanh toán (isPaid: true).'),
    numberedItem('(End) Đơn hàng được cập nhật, thông báo gửi cho khách hàng.'),
    spacer(),

    h4('3.2.2.4. Quản lý khách hàng'),
    p('Admin xem danh sách người dùng với phân khúc tự động: VIP (10+ đơn), Regular (3+), New (1 đơn), Inactive (0 đơn). Xem chi tiết bao gồm hồ sơ, lịch sử đơn hàng, tổng chi tiêu. Admin có thể ban user nếu cần thiết.', { indent: true }),
    spacer(),

    h4('3.2.2.5. Quản lý các bài đăng'),
    p('Admin quản lý hai loại bài đăng: (1) Blog — CRUD bài viết tin tức với trạng thái nháp/xuất bản; (2) AuraHub — kiểm duyệt bài đăng cộng đồng: xem danh sách bài pending → duyệt (approve) hoặc từ chối (reject với lý do) hoặc xóa.', { indent: true }),
    spacer(),

    h4('3.2.2.6. Quản lý chương trình khuyến mãi'),
    p('Admin CRUD mã voucher khuyến mãi với các thuộc tính: mã code (uppercase), phần trăm giảm (1-100%), giá trị giảm tối đa, đơn hàng tối thiểu, giới hạn tổng lượt dùng, giới hạn lượt/người, ngày bắt đầu và kết thúc, trạng thái active. Hệ thống tự động validate khi khách hàng áp dụng mã.', { indent: true }),
    spacer(),

    h4('3.2.2.7. Quản lý hỗ trợ khách hàng'),
    p('Admin xem danh sách cuộc hội thoại hỗ trợ (tab: đang mở / đã lưu trữ), trả lời tin nhắn real-time qua Socket.IO, đánh dấu đã đọc, lưu trữ cuộc trò chuyện khi hoàn tất. Typing indicator hiển thị khi admin đang gõ.', { indent: true }),
    spacer(),

    // ===== 3.4 DFD =====
    h2('3.4. DFD (Data Flow Diagram)'),

    h3('3.4.1. Context DFD'),
    p('Context Diagram (Biểu đồ ngữ cảnh) mô tả hệ thống AuraPC như một tiến trình duy nhất (Process 0) tương tác với 6 tác nhân ngoài (External Entities):', { indent: true }),
    p('**Tiến trình trung tâm:** 0 — Hệ thống AuraPC', { indent: true }),
    p('**Các tác nhân ngoài:**', { indent: true }),
    numberedItem('**CUSTOMER (Khách hàng):** Gửi thông tin xác thực, dữ liệu mua hàng, tương tác nội dung. Nhận kết quả xác thực, thông tin sản phẩm, nội dung và thông báo.'),
    numberedItem('**ADMINISTRATOR (Quản trị viên):** Gửi thông tin đăng nhập, quản lý và xử lý nghiệp vụ. Nhận JWT, dashboard data, thông báo admin.'),
    numberedItem('**PAYMENT GATEWAY (Cổng thanh toán — MoMo/ZaloPay):** Nhận yêu cầu tạo giao dịch. Trả kết quả thanh toán qua URL và IPN callback.'),
    numberedItem('**EMAIL SERVICE (Gmail SMTP/Nodemailer):** Nhận yêu cầu gửi email hóa đơn đơn hàng và PDF cấu hình Builder.'),
    numberedItem('**AI CHATBOT SERVICE (Replicate — Qwen 3 235B):** Nhận tin nhắn + lịch sử chat + catalog sản phẩm. Trả phản hồi AI + danh sách sản phẩm gợi ý.'),
    numberedItem('**VIETNAM ADDRESS API (provinces.open-api.vn):** Nhận yêu cầu mã tỉnh/quận. Trả dữ liệu địa chỉ Việt Nam (tỉnh, quận, phường).'),
    p('Tổng cộng: **17 luồng dữ liệu** (data flows) giữa hệ thống và các tác nhân ngoài. File draw.io chi tiết được lưu tại: docs/DFD-Level0-Context-Diagram-EN.drawio.', { indent: true }),
    spacer(),

    h3('3.4.2. DFD Level 0'),
    p('DFD Level 0 phân rã tiến trình trung tâm thành các tiến trình con chính:', { indent: true }),
    makeTable(
      [1200, 3200, 5000],
      ['Process', 'Tên tiến trình', 'Mô tả'],
      [
        ['1.0', 'Quản lý xác thực', 'Xử lý đăng ký/đăng nhập OTP, quản lý JWT token, hồ sơ người dùng'],
        ['2.0', 'Quản lý sản phẩm', 'Hiển thị, tìm kiếm, lọc sản phẩm; quản lý danh mục; đánh giá & bình luận'],
        ['3.0', 'Quản lý giỏ hàng', 'Thêm/xóa/cập nhật sản phẩm trong giỏ, đồng bộ local-server, kiểm tra tồn kho'],
        ['4.0', 'Xử lý đơn hàng', 'Tạo đơn, cập nhật trạng thái, hủy/trả, theo dõi, gửi thông báo'],
        ['5.0', 'Xử lý thanh toán', 'Tích hợp MoMo, ZaloPay, COD+OTP, ATM/QR; xác thực IPN callback'],
        ['6.0', 'Quản lý nội dung', 'Blog, AuraHub (posts/comments/likes), FAQ, trang thương hiệu'],
        ['7.0', 'Aura Builder', 'Lắp ráp cấu hình PC, lưu/chia sẻ config, xuất PDF, gửi email'],
        ['8.0', 'AI Chatbot (AruBot)', 'Xử lý tin nhắn, gọi Replicate API, matching sản phẩm, gợi ý'],
        ['9.0', 'Hỗ trợ khách hàng', 'Chat real-time, quản lý hội thoại, typing indicator'],
        ['10.0', 'Quản trị hệ thống', 'Dashboard, CRUD tất cả entities, kiểm duyệt, thống kê doanh thu'],
      ]
    ),
    p('Các kho dữ liệu (Data Stores) chính: D1-User, D2-Product, D3-Category, D4-Order, D5-Cart, D6-Blog, D7-Post, D8-Builder, D9-Promotion, D10-Notification, D11-SupportConversation, D12-Otp, D13-ProductReview.', { indent: true }),
    spacer(),

    // ===== 3.5 DATABASE =====
    h2('3.5. Thiết kế cơ sở dữ liệu trên MongoDB'),

    h3('3.5.1. Định nghĩa các thực thể'),
    p('Hệ thống AuraPC sử dụng MongoDB với **20 Mongoose models** (thực thể), tổ chức thành các nhóm chức năng:', { indent: true }),

    p('**Nhóm Người dùng & Xác thực (3 thực thể):**', { indent: true }),
    bullet('**User:** Thông tin người dùng, hồ sơ, địa chỉ, mạng xã hội AuraHub (followers/following).'),
    bullet('**Admin:** Quản trị viên hệ thống, đăng nhập email/password (bcrypt).'),
    bullet('**Otp:** Mã xác thực OTP 6 số, tự xóa sau 5 phút (TTL index).'),
    spacer(),

    p('**Nhóm Sản phẩm & Danh mục (3 thực thể):**', { indent: true }),
    bullet('**Product:** Thông tin sản phẩm với schema linh hoạt (Mixed type cho specs/images).'),
    bullet('**Category:** Danh mục sản phẩm đa cấp (parent-child hierarchy).'),
    bullet('**ProductReview:** Đánh giá (1-5 sao) và bình luận, hỗ trợ phản hồi lồng (nested reply).'),
    spacer(),

    p('**Nhóm Đơn hàng & Thanh toán (4 thực thể):**', { indent: true }),
    bullet('**Order:** Đơn hàng với embedded OrderItem, ShippingAddress, RequestState (hủy/trả).'),
    bullet('**Cart:** Giỏ hàng (1 user = 1 cart, unique constraint).'),
    bullet('**Promotion:** Mã khuyến mãi với validation phức tạp (ngày, giới hạn, đơn tối thiểu).'),
    bullet('**PromotionUsage:** Ghi nhận lịch sử sử dụng mã khuyến mãi theo user.'),
    spacer(),

    p('**Nhóm Cộng đồng AuraHub (3 thực thể):**', { indent: true }),
    bullet('**Post:** Bài đăng với poll, repost, kiểm duyệt, lên lịch đăng.'),
    bullet('**HubComment:** Bình luận lồng (nested reply) với like.'),
    bullet('**Share:** Ghi nhận lượt chia sẻ bài đăng (copy_link, copy_image, embed).'),
    spacer(),

    p('**Nhóm Nội dung & Hỗ trợ (5 thực thể):**', { indent: true }),
    bullet('**Blog:** Bài viết tin tức, hỗ trợ nháp/xuất bản.'),
    bullet('**Builder:** Cấu hình PC Builder, tự xóa sau 7 ngày (TTL index), chia sẻ qua shareId.'),
    bullet('**SupportConversation:** Hội thoại hỗ trợ 1-1 (1 user = 1 conversation).'),
    bullet('**SupportMessage:** Tin nhắn trong hội thoại, phân biệt user/admin sender.'),
    bullet('**Faq:** Câu hỏi thường gặp, phân loại theo chủ đề.'),
    spacer(),

    p('**Nhóm Thông báo (2 thực thể):**', { indent: true }),
    bullet('**UserNotification:** Thông báo cho người dùng (9 loại enum liên quan đến đơn hàng).'),
    bullet('**AdminNotification:** Thông báo cho admin (4 loại: đơn mới, yêu cầu hủy/trả, đơn delivered).'),
    spacer(),

    h3('3.5.2. Xác định các thuộc tính và kiểu dữ liệu của các thực thể'),
    p('Chi tiết đầy đủ về thuộc tính, kiểu dữ liệu và ràng buộc của tất cả 20 thực thể được trình bày trong tài liệu đính kèm: **"AuraPC-Dac-Ta-Thuc-The-Du-Lieu.docx"** (đã xuất riêng). Dưới đây là tóm tắt các thực thể quan trọng nhất:', { indent: true }),
    spacer(),

    p('**Thực thể User (Người dùng) — 19 thuộc tính:**', { indent: true }),
    makeTable(
      [2600, 2000, 4760],
      ['Thuộc tính chính', 'Kiểu dữ liệu', 'Ràng buộc / Mô tả'],
      [
        ['phoneNumber', 'String', 'Required, Unique — SĐT đăng nhập (0xxx hoặc 84xxx)'],
        ['profile (fullName, dateOfBirth, gender)', 'Embedded Object', 'Thông tin cá nhân'],
        ['addresses', '[Address sub-schema]', 'DS địa chỉ giao hàng (label, fullName, phone, city, district, ward, address, isDefault)'],
        ['followers / following', '[ObjectId → User]', 'Mạng xã hội AuraHub'],
        ['active', 'Boolean', 'Default: true — trạng thái tài khoản'],
      ]
    ),
    spacer(),

    p('**Thực thể Order (Đơn hàng) — 30 thuộc tính:**', { indent: true }),
    makeTable(
      [2600, 2000, 4760],
      ['Thuộc tính chính', 'Kiểu dữ liệu', 'Ràng buộc / Mô tả'],
      [
        ['orderNumber', 'String', 'Required, Unique — Mã đơn hiển thị'],
        ['items', '[OrderItem]', 'Embedded: product (ObjectId), name, price, qty'],
        ['status', 'String (Enum)', 'pending | confirmed | processing | shipped | delivered | cancelled'],
        ['paymentMethod', 'String (Enum)', 'cod | qr | momo | zalopay | atm'],
        ['cancelRequest / returnRequest', 'RequestState', 'Embedded: status, reason, requestedAt, resolvedAt, resolvedBy, note'],
        ['total, shippingFee, discount', 'Number', 'Tổng tiền, phí ship, giảm giá (VND)'],
      ]
    ),
    spacer(),

    p('**Thực thể Product (Sản phẩm) — 23 thuộc tính:**', { indent: true }),
    makeTable(
      [2600, 2000, 4760],
      ['Thuộc tính chính', 'Kiểu dữ liệu', 'Ràng buộc / Mô tả'],
      [
        ['name / slug', 'String', 'Required — Tên SP và đường dẫn URL'],
        ['price / salePrice / old_price', 'Number', 'Giá gốc, giá KM, giá cũ (VND)'],
        ['specs / techSpecs', 'Mixed (Object)', 'Thông số kỹ thuật linh hoạt theo loại SP'],
        ['images', 'Mixed', 'DS hình ảnh sản phẩm'],
        ['stock', 'Number', 'Default: 0 — Số lượng tồn kho'],
        ['brand / category', 'String / Mixed', 'Thương hiệu và danh mục'],
      ]
    ),
    spacer(),
    p('Danh sách đầy đủ 20 thực thể với tất cả thuộc tính, kiểu dữ liệu, ràng buộc, enum values, mối quan hệ và sub-schema đã được xuất trong file riêng.', { indent: true }),
  ];
};
