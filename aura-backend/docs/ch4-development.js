const { h1, h2, h3, h4, p, bullet, numberedItem, spacer, pageBreak, makeTable, figureCaption } = require('./report-helpers');

module.exports = function chapter4() {
  return [
    h1('CHƯƠNG 4: TỔ CHỨC VÀ XÂY DỰNG WEBSITE'),
    pageBreak(),

    // ===== 4.1 =====
    h2('4.1. Bộ nhận diện thương hiệu'),

    h3('4.1.1. Logo'),
    p('Logo AuraPC được thiết kế với phong cách hiện đại, tối giản, phản ánh tinh thần công nghệ và gaming. Logo sử dụng hai phiên bản:', { indent: true }),
    bullet('**Light Mode Logo:** Logo chính trên nền sáng, sử dụng tông màu tối (đen/xanh đậm) với hiệu ứng gradient tinh tế, tạo cảm giác chuyên nghiệp và tin cậy.'),
    bullet('**Dark Mode Logo:** Phiên bản trắng cho nền tối, đảm bảo tính nhất quán thương hiệu trên cả hai chế độ hiển thị.'),
    p('Logo được sử dụng nhất quán trên toàn bộ hệ thống: header website khách hàng, sidebar Admin Panel, favicon, email template, PDF invoice và PC Builder export.', { indent: true }),
    spacer(),

    h3('4.1.2. Header & Footer'),
    p('**Header** được thiết kế sticky (cố định khi cuộn), bao gồm các thành phần:', { indent: true }),
    bullet('Logo thương hiệu (bên trái) — click để về trang chủ.'),
    bullet('Thanh điều hướng chính: Sản phẩm, Aura Builder, AuraHub, Blog, Hỗ trợ.'),
    bullet('Thanh tìm kiếm: Tìm kiếm sản phẩm theo từ khóa với autocomplete.'),
    bullet('Biểu tượng chức năng (bên phải): Giỏ hàng (badge số lượng), Thông báo (badge), Tài khoản (avatar/login).'),
    bullet('Responsive: Thu gọn thành hamburger menu trên mobile, bottom navigation bar.'),
    p('**Footer** bao gồm:', { indent: true }),
    bullet('Thông tin liên hệ: địa chỉ, email, số điện thoại.'),
    bullet('Danh mục sản phẩm: Liên kết nhanh đến các danh mục chính.'),
    bullet('Chính sách: Bảo hành, đổi trả, vận chuyển, thanh toán.'),
    bullet('Kết nối mạng xã hội: Facebook, Instagram, YouTube, TikTok.'),
    bullet('Bản quyền và thông tin pháp lý.'),
    spacer(),

    h3('4.1.3. Font'),
    p('Hệ thống typography của AuraPC sử dụng:', { indent: true }),
    bullet('**Font chính — Inter:** Sans-serif hiện đại, được tối ưu cho màn hình digital. Sử dụng cho nội dung văn bản, UI elements, navigation. Inter có bộ ký tự hỗ trợ tiếng Việt đầy đủ với dấu rõ ràng ở mọi kích thước.'),
    bullet('**Font phụ — Space Grotesk / Orbitron:** Dùng cho tiêu đề, banner, elements mang tính gaming/tech để tạo điểm nhấn.'),
    bullet('**Font monospace — JetBrains Mono / Fira Code:** Dùng cho hiển thị thông số kỹ thuật sản phẩm, mã đơn hàng, code blocks.'),
    p('Hệ thống kích thước font được chuẩn hóa:', { indent: true }),
    makeTable(
      [2400, 2400, 4560],
      ['Vai trò', 'Kích thước', 'Sử dụng'],
      [
        ['Display / Hero', '36–48px', 'Banner trang chủ, tiêu đề lớn'],
        ['Heading 1', '28–32px', 'Tiêu đề trang, section heading'],
        ['Heading 2', '22–24px', 'Sub-section heading'],
        ['Heading 3', '18–20px', 'Card title, widget heading'],
        ['Body', '14–16px', 'Nội dung chính, mô tả sản phẩm'],
        ['Small / Caption', '12–13px', 'Thông tin phụ, metadata, helper text'],
      ]
    ),
    spacer(),

    h3('4.1.4. Color'),
    p('Bảng màu AuraPC được xây dựng với mục tiêu: chuyên nghiệp, hiện đại, phù hợp với gaming và dễ đọc trên mọi thiết bị.', { indent: true }),
    makeTable(
      [2400, 2000, 2000, 2960],
      ['Vai trò', 'Mã màu', 'Tên', 'Sử dụng'],
      [
        ['Primary', '#2563EB', 'Royal Blue', 'CTA buttons, links, active states, brand accent'],
        ['Secondary', '#7C3AED', 'Purple', 'Gradient accent, AuraHub, premium features'],
        ['Success', '#10B981', 'Emerald', 'Trạng thái thành công, delivered, active'],
        ['Warning', '#F59E0B', 'Amber', 'Cảnh báo, pending status, low stock'],
        ['Error', '#EF4444', 'Red', 'Lỗi validation, cancelled, out of stock'],
        ['Background (Light)', '#FFFFFF / #F9FAFB', 'White/Gray-50', 'Nền trang, nền card'],
        ['Background (Dark)', '#111827 / #1F2937', 'Gray-900/800', 'Dark mode background'],
        ['Text Primary', '#111827', 'Gray-900', 'Văn bản chính'],
        ['Text Secondary', '#6B7280', 'Gray-500', 'Văn bản phụ, placeholder'],
      ]
    ),
    p('Admin Panel sử dụng hệ màu Shopify Polaris-inspired riêng biệt, hỗ trợ chuyển đổi Dark/Light theme thông qua ThemeService (Angular Signal).', { indent: true }),
    spacer(),

    h3('4.1.5. Components'),
    p('Hệ thống component UI được xây dựng theo nguyên tắc Atomic Design, chia thành các cấp độ:', { indent: true }),
    p('**Atoms (Thành phần cơ bản):**', { indent: true }),
    bullet('Button: Primary, Secondary, Ghost, Danger — với loading state và disabled state.'),
    bullet('Input: Text, Number, Phone, Email, Password — với validation và error message.'),
    bullet('Badge: Status badges (pending, confirmed, shipped, delivered, cancelled) với màu sắc phân biệt.'),
    bullet('Avatar: Hiển thị ảnh đại diện với fallback (chữ cái đầu tên).'),
    bullet('Toast: Thông báo tạm thời (success, error, warning, info) tự ẩn sau 3-5 giây.'),
    spacer(),
    p('**Molecules (Thành phần kết hợp):**', { indent: true }),
    bullet('Product Card: Ảnh, tên, giá (gốc + sale), badge (sale %, new, hot), nút thêm giỏ hàng.'),
    bullet('Search Bar: Input + dropdown gợi ý sản phẩm real-time.'),
    bullet('Address Card: Hiển thị địa chỉ giao hàng với nút sửa/xóa/đặt mặc định.'),
    bullet('Order Status Stepper: Timeline hiển thị tiến trình đơn hàng (5 bước).'),
    bullet('Rating Stars: Hiển thị và input đánh giá 1-5 sao.'),
    spacer(),
    p('**Organisms (Thành phần phức tạp):**', { indent: true }),
    bullet('Navigation Bar: Header responsive với mega menu, search, cart, notifications.'),
    bullet('Product Grid: Lưới sản phẩm responsive (4 cột desktop, 2 tablet, 1 mobile).'),
    bullet('Checkout Form: Multi-step form (địa chỉ → mã KM → thanh toán → xác nhận).'),
    bullet('Chat Widget: Floating chatbot widget (AruBot) với message history.'),
    bullet('Builder Panel: PC configurator với danh sách component steps và tổng giá.'),
    spacer(),

    h3('4.1.6. Mockup'),
    p('Mockup được thiết kế trên Figma, mô phỏng giao diện thực tế của website trên các thiết bị khác nhau. Các mockup chính bao gồm:', { indent: true }),
    bullet('Desktop mockup (1920x1080): Trang chủ với 3D canvas, danh sách sản phẩm, chi tiết sản phẩm, checkout, admin dashboard.'),
    bullet('Tablet mockup (768x1024): Layout 2 cột, navigation thu gọn, touch-optimized UI.'),
    bullet('Mobile mockup (375x812): Layout 1 cột, bottom navigation, swipe gestures, full-screen product viewer.'),
    spacer(),

    h3('4.1.7. Wireframe'),

    h4('4.1.7.1. Wireframe người dùng'),
    p('Wireframe người dùng mô tả bố cục cơ bản (layout) cho các trang chính, tập trung vào cấu trúc nội dung và luồng tương tác:', { indent: true }),
    bullet('**Trang chủ:** Hero section (3D canvas) → Product showcase → Categories → Blog highlights → Footer.'),
    bullet('**Trang sản phẩm:** Sidebar bộ lọc (trái) + Grid sản phẩm (phải) + Pagination.'),
    bullet('**Chi tiết SP:** Gallery ảnh (trái) + Thông tin & mua hàng (phải) + Tabs (mô tả, specs, đánh giá).'),
    bullet('**Giỏ hàng:** Danh sách sản phẩm + Mã KM + Tổng cộng + CTA checkout.'),
    bullet('**Checkout:** Stepper (3 bước) + Form địa chỉ + Chọn thanh toán + Order summary.'),
    bullet('**Tài khoản:** Tab navigation: Hồ sơ | Địa chỉ | Đơn hàng | Thông báo.'),
    spacer(),

    h4('4.1.7.2. Wireframe quản trị viên'),
    p('Wireframe Admin Panel thiết kế theo layout sidebar + main content:', { indent: true }),
    bullet('**Layout chung:** Sidebar điều hướng (trái, 220px collapsible) + Header (top) + Main content area (phải).'),
    bullet('**Dashboard:** 4 KPI cards (top) + Biểu đồ doanh thu (giữa) + Đơn hàng gần đây (dưới) + Top sản phẩm.'),
    bullet('**Trang quản lý (Products, Orders, Users, Blogs):** Search/Filter bar + Data table + Pagination + Action buttons.'),
    bullet('**Form CRUD:** Two-column layout cho form thêm/sửa sản phẩm, blog.'),
    spacer(),

    // ===== 4.2 =====
    h2('4.2. Giao diện website (Frontend)'),
    p('Phần này trình bày chi tiết giao diện các trang chính của website AuraPC, bao gồm mô tả chức năng, thành phần UI và trải nghiệm người dùng. Tất cả giao diện đều được xây dựng bằng Angular 21 với standalone components và CSS responsive.', { indent: true }),

    h3('4.2.1. Giao diện dành cho khách hàng'),

    h4('4.2.1.1. Trang Đăng ký'),
    p('Trang đăng ký/đăng nhập AuraPC sử dụng phương thức xác thực OTP qua số điện thoại, thay vì email/password truyền thống — phù hợp với thói quen sử dụng của người dùng Việt Nam.', { indent: true }),
    p('**Giao diện gồm 2 bước:**', { indent: true }),
    numberedItem('**Bước 1 — Nhập số điện thoại:** Form đơn giản với ô input SĐT (validation format 0xxxxxxxxx hoặc +84xxxxxxxxx), nút "Gửi mã OTP" (CTA primary). Hỗ trợ keyboard: Enter để submit.'),
    numberedItem('**Bước 2 — Nhập OTP:** 6 ô input riêng biệt (OTP input style) với auto-focus chuyển ô tiếp theo khi nhập. Countdown 60 giây trước khi cho phép gửi lại OTP. Nút "Xác nhận" và "Gửi lại mã".'),
    p('**Đặc điểm UI:** Dialog overlay (modal popup) thay vì trang riêng biệt, cho phép đăng nhập từ bất kỳ trang nào mà không mất ngữ cảnh. Animation chuyển đổi mượt mà giữa 2 bước.', { indent: true }),
    spacer(),

    h4('4.2.1.2. Trang Đăng nhập'),
    p('Trang đăng nhập sử dụng cùng component với trang đăng ký (AuthComponent), vì AuraPC áp dụng phương thức "passwordless authentication" — người dùng chỉ cần số điện thoại + OTP, không cần tạo mật khẩu. Hệ thống tự động phân biệt đăng ký (SĐT mới) và đăng nhập (SĐT đã tồn tại) dựa trên database lookup.', { indent: true }),
    spacer(),

    h4('4.2.1.3. Trang Chủ'),
    p('Trang chủ AuraPC là điểm nhấn quan trọng nhất, kết hợp giữa 3D visualization và content marketing:', { indent: true }),
    p('**Các section chính (theo thứ tự từ trên xuống):**', { indent: true }),
    numberedItem('**Hero Section (3D Canvas):** Sử dụng Three.js để render một scene 3D tương tác: mô hình PC gaming có thể xoay, hiệu ứng ánh sáng động, hotspot interaction (click vào điểm trên mô hình để xem thông tin linh kiện). Canvas chiếm toàn bộ viewport ban đầu với overlay text "Build Your Dream PC" và CTA button dẫn đến Aura Builder.'),
    numberedItem('**Sản phẩm nổi bật:** Carousel/Grid hiển thị các sản phẩm featured, best-seller với Product Card component: ảnh, tên, giá gốc (gạch ngang) + giá sale (đỏ, phần trăm giảm), badge "Hot", "New", nút "Thêm giỏ hàng".'),
    numberedItem('**Danh mục sản phẩm:** Grid icon-based hiển thị các danh mục chính (PC, Laptop, GPU, CPU, Monitor, Peripherals) với hover effect, click navigate đến trang sản phẩm tương ứng.'),
    numberedItem('**Aura Builder CTA:** Banner lớn giới thiệu công cụ PC Builder với hiệu ứng glowing border animation, text mô tả và CTA button.'),
    numberedItem('**Blog Highlights:** 3-4 bài viết mới nhất với thumbnail, tiêu đề, ngày đăng.'),
    numberedItem('**AuraHub Preview:** Hiển thị bài đăng trending từ cộng đồng.'),
    spacer(),

    h4('4.2.1.4. Trang Danh sách sản phẩm'),
    p('Trang sản phẩm (/san-pham) cung cấp trải nghiệm duyệt và tìm kiếm sản phẩm toàn diện:', { indent: true }),
    p('**Layout:**', { indent: true }),
    bullet('**Sidebar bộ lọc (trái, desktop):** Danh mục (category tree), Thương hiệu (checkbox list), Khoảng giá (slider hoặc input), Thông số kỹ thuật tùy theo danh mục (ví dụ: Socket CPU cho mainboard, VRAM cho GPU).'),
    bullet('**Product Grid (phải):** Hiển thị sản phẩm dạng grid responsive (4 cột desktop → 2 tablet → 1 mobile). Mỗi product card hiển thị: ảnh thumbnail, tên, giá (có gạch ngang giá gốc nếu sale), badge (% giảm, New, Hot), nút thêm giỏ hàng.'),
    bullet('**Thanh công cụ (top):** Sort dropdown (giá tăng/giảm, mới nhất, bán chạy), toggle view (grid/list), hiển thị số kết quả, breadcrumb navigation.'),
    bullet('**Pagination:** Phân trang với page numbers, previous/next buttons.'),
    p('**Tìm kiếm:** Input search trên header với debounce 300ms, hiển thị dropdown gợi ý sản phẩm match (tên + ảnh + giá). URL query params: /san-pham?search=keyword&category=gpu&brand=nvidia&sort=price_asc&page=1.', { indent: true }),
    spacer(),

    h4('4.2.1.5. Trang Chi tiết sản phẩm'),
    p('Trang chi tiết (/san-pham/:slug) hiển thị đầy đủ thông tin sản phẩm:', { indent: true }),
    p('**Phần trên (Above the fold):**', { indent: true }),
    bullet('**Gallery ảnh (trái):** Ảnh chính lớn + thumbnail strip bên dưới, click để xem ảnh lớn, hỗ trợ zoom on hover. Nếu sản phẩm có mô hình 3D: hiển thị Google Model Viewer (<model-viewer>) với khả năng xoay 360 độ.'),
    bullet('**Thông tin mua hàng (phải):** Tên sản phẩm, thương hiệu, rating trung bình (sao), giá (gốc + sale), trạng thái tồn kho (In stock/Out of stock với số lượng), quantity selector, nút "Thêm vào giỏ hàng" (CTA primary), nút "Mua ngay" (redirect checkout).'),
    p('**Phần dưới (Tab navigation):**', { indent: true }),
    bullet('**Tab Mô tả:** Rich HTML content mô tả sản phẩm (đã sanitize).'),
    bullet('**Tab Thông số kỹ thuật:** Bảng key-value hiển thị specs chi tiết (CPU: clock speed, cores; GPU: VRAM, chipset; RAM: capacity, speed, type).'),
    bullet('**Tab Đánh giá:** Tổng rating (sao trung bình + phân bố 1-5 sao dạng bar chart), danh sách reviews/comments, form viết đánh giá (nếu đã mua), reply thread.'),
    spacer(),

    h4('4.2.1.6. Trang Giỏ hàng'),
    p('Trang giỏ hàng (/cart) hiển thị danh sách sản phẩm đã chọn với khả năng quản lý:', { indent: true }),
    bullet('**Danh sách sản phẩm:** Mỗi item hiển thị: ảnh thumbnail, tên SP (link đến chi tiết), đơn giá, quantity selector (- / số / +), thành tiền, nút xóa.'),
    bullet('**Mã khuyến mãi:** Input nhập mã voucher + nút "Áp dụng". Hiển thị thông tin giảm giá nếu mã hợp lệ (phần trăm giảm, số tiền giảm).'),
    bullet('**Tóm tắt đơn hàng:** Tạm tính, giảm giá, phí vận chuyển (nếu có), tổng cộng (VND format).'),
    bullet('**CTA:** Nút "Thanh toán" (redirect checkout), nút "Tiếp tục mua sắm" (back to products).'),
    bullet('**Empty state:** Khi giỏ hàng trống: hiển thị illustration, text "Giỏ hàng trống", CTA "Khám phá sản phẩm".'),
    spacer(),

    h4('4.2.1.7. Trang Đặt hàng'),
    p('Trang checkout (/checkout) là quy trình multi-step để hoàn tất đơn hàng:', { indent: true }),
    p('**Bước 1 — Địa chỉ giao hàng:**', { indent: true }),
    bullet('Danh sách địa chỉ đã lưu (radio select) với badge "Mặc định".'),
    bullet('Form thêm địa chỉ mới: Họ tên, SĐT, dropdown Tỉnh/Thành → Quận/Huyện → Phường/Xã (cascade, dữ liệu từ provinces.open-api.vn), địa chỉ chi tiết.'),
    p('**Bước 2 — Phương thức thanh toán:**', { indent: true }),
    bullet('Radio group 5 lựa chọn: COD (thanh toán khi nhận hàng, yêu cầu OTP xác nhận), MoMo (redirect sang app MoMo), ZaloPay (redirect sang ZaloPay), Chuyển khoản ATM (hiển thị thông tin tài khoản), QR Code.'),
    p('**Bước 3 — Xác nhận đơn hàng:**', { indent: true }),
    bullet('Order summary: Danh sách SP, địa chỉ giao, phương thức TT, mã KM, tổng tiền.'),
    bullet('Nút "Đặt hàng" (CTA primary) — trigger tạo đơn hàng hoặc redirect thanh toán.'),
    spacer(),

    h4('4.2.1.8. Trang Theo dõi đơn hàng'),
    p('Trang tra cứu đơn hàng (/tra-cuu-don-hang) cho phép cả guest và registered user kiểm tra trạng thái đơn:', { indent: true }),
    bullet('**Input tra cứu:** Nhập mã đơn hàng (orderNumber) → Tìm kiếm.'),
    bullet('**Kết quả:** Order Status Stepper hiển thị tiến trình 5 bước: Đặt hàng → Xác nhận → Đang xử lý → Đang giao → Đã giao. Mỗi bước có timestamp. Bên dưới: chi tiết sản phẩm, địa chỉ giao, tổng tiền.'),
    bullet('**Real-time update:** Socket.IO tự động cập nhật trạng thái khi admin thay đổi (không cần reload).'),
    spacer(),

    h4('4.2.1.9. Trang Đánh giá'),
    p('Trang đánh giá sản phẩm được tích hợp trong tab "Đánh giá" trên trang chi tiết sản phẩm:', { indent: true }),
    bullet('**Overview:** Rating trung bình (sao lớn), tổng số đánh giá, phân bố 1-5 sao (horizontal bar chart).'),
    bullet('**Bộ lọc:** Filter theo: Tất cả, Có ảnh, Chỉ review (có sao), Chỉ bình luận, Theo số sao. Sort: Mới nhất, Cũ nhất.'),
    bullet('**Danh sách reviews:** Mỗi review hiển thị: avatar + tên user, ngày đăng, số sao (nếu là review), nội dung text, ảnh đính kèm (gallery), reply thread (nested).'),
    bullet('**Form viết đánh giá:** Kiểm tra quyền (đã mua + đã nhận hàng) → Chọn loại (Review/Comment) → Chọn sao → Viết nội dung → Upload ảnh → Submit.'),
    spacer(),

    h3('4.2.2. Giao diện dành cho quản trị viên'),
    p('Admin Panel (port 4201) được thiết kế với phong cách Shopify Polaris-inspired, hỗ trợ Dark/Light theme:', { indent: true }),

    h4('4.2.1.1. Trang Đăng nhập'),
    p('Trang đăng nhập Admin (/login) có giao diện đơn giản, chuyên nghiệp:', { indent: true }),
    bullet('Logo AuraPC (phiên bản tương ứng với theme hiện tại).'),
    bullet('Form đăng nhập: Email input + Password input (toggle show/hide).'),
    bullet('Nút "Đăng nhập" — gọi POST /api/admin/auth/login.'),
    bullet('Thông báo lỗi (email/mật khẩu sai) hiển thị inline dưới form.'),
    bullet('Sau đăng nhập thành công: redirect Dashboard, kết nối Socket.IO room "admin".'),
    spacer(),

    h4('4.2.2.2. Trang Quản lý sản phẩm'),
    p('Trang Products (/products) hiển thị danh sách sản phẩm dạng bảng với đầy đủ chức năng quản lý:', { indent: true }),
    bullet('**Thanh công cụ:** Search by name, filter by category/status/stock, nút "Thêm sản phẩm mới".'),
    bullet('**Data table:** Columns: Ảnh thumbnail, Tên SP, Danh mục, Giá, Tồn kho, Trạng thái (Active/Inactive badge), Actions (Edit/Delete/Toggle).'),
    bullet('**Stock indicators:** Màu sắc phân biệt tồn kho: đỏ (hết hàng, 0), vàng (sắp hết, 1-5), xanh (còn hàng, 6+).'),
    bullet('**Form thêm/sửa SP:** Multi-section form: Thông tin cơ bản (tên, slug, mô tả) → Giá và tồn kho → Danh mục và thương hiệu → Thông số kỹ thuật (dynamic key-value) → Upload ảnh.'),
    spacer(),

    h4('4.2.2.3. Trang Quản lý đơn hàng'),
    p('Trang Orders (/orders) là trang quan trọng nhất của Admin Panel:', { indent: true }),
    bullet('**Bộ lọc tab:** Tabs theo trạng thái: Tất cả | Chờ xử lý | Đã xác nhận | Đang xử lý | Đang giao | Đã giao | Đã hủy.'),
    bullet('**Data table:** Mã đơn, Khách hàng (tên + SĐT), Ngày đặt, Tổng tiền, Thanh toán (badge: MoMo/ZaloPay/COD/ATM), Trạng thái (color-coded badge), Actions.'),
    bullet('**Chi tiết đơn hàng:** Stepper tiến trình + Thông tin khách + Danh sách SP + Địa chỉ giao + Lịch sử trạng thái (timeline với timestamps).'),
    bullet('**Actions:** Cập nhật trạng thái (dropdown), xác nhận thanh toán COD, duyệt/từ chối yêu cầu hủy hoặc trả hàng (với dialog nhập ghi chú).'),
    bullet('**Real-time:** Socket.IO nhận thông báo đơn hàng mới, bell icon với badge count.'),
    spacer(),

    h4('4.2.2.4. Trang Quản lý khách hàng'),
    p('Trang Users (/users) quản lý danh sách người dùng đã đăng ký:', { indent: true }),
    bullet('**Bộ lọc:** Phân khúc tự động: VIP (10+ đơn), Regular (3-9 đơn), New (1-2 đơn), Inactive (0 đơn). Search by tên/SĐT.'),
    bullet('**Data table:** Avatar, Họ tên, SĐT, Email, Phân khúc (badge), Tổng đơn, Tổng chi tiêu, Ngày tham gia, Actions.'),
    bullet('**Chi tiết user:** Hồ sơ đầy đủ + Thống kê mua hàng + Danh sách đơn hàng + Lịch sử hoạt động.'),
    spacer(),

    h4('4.2.2.5. Trang Quản lý các bài đăng'),
    p('Quản lý hai loại nội dung:', { indent: true }),
    bullet('**Blog (/blogs):** CRUD bài viết tin tức. Data table: Tiêu đề, Tác giả, Ngày tạo, Trạng thái (Nháp/Xuất bản), Actions (Edit/Delete). Form editor: Title, slug, content (rich text), excerpt, images, published toggle.'),
    bullet('**AuraHub (/hub):** Kiểm duyệt bài đăng cộng đồng. Tabs: Chờ duyệt | Đã xuất bản. Preview bài đăng (nội dung, ảnh, poll) + Actions: Duyệt (publish) | Từ chối (delete với lý do).'),
    spacer(),

    h4('4.2.2.7. Trang Quản lý chăm sóc khách hàng'),
    p('Trang Support (/support) cho phép admin chat real-time với khách hàng:', { indent: true }),
    bullet('**Layout 2 cột:** Danh sách hội thoại (trái) + Chat window (phải).'),
    bullet('**Danh sách hội thoại:** Tabs: Đang mở | Đã lưu trữ. Mỗi item: Avatar + Tên khách + Tin nhắn cuối + Thời gian + Unread badge.'),
    bullet('**Chat window:** Message bubbles (phân biệt user/admin), input gửi tin nhắn, typing indicator khi admin đang gõ, nút lưu trữ (archive) cuộc trò chuyện.'),
    bullet('**Real-time:** Socket.IO cho tin nhắn mới, typing indicator, conversation updates.'),
    spacer(),

    h4('4.2.2.8. Trang Báo cáo và thống kê'),
    p('Dashboard (/) là trang mặc định sau đăng nhập, cung cấp cái nhìn tổng quan về hoạt động kinh doanh:', { indent: true }),
    bullet('**KPI Cards (4 cards top):** Tổng doanh thu (tháng), Số đơn hàng (tháng), Số khách hàng mới (tháng), Tỷ lệ chuyển đổi. Mỗi card có so sánh % với tháng trước (tăng xanh/giảm đỏ).'),
    bullet('**Biểu đồ doanh thu:** Line chart hoặc bar chart hiển thị doanh thu theo ngày/tuần/tháng (7-12 tháng gần nhất).'),
    bullet('**Đơn hàng gần đây:** Data table 5-10 đơn mới nhất với quick actions.'),
    bullet('**Top sản phẩm bán chạy:** Bảng xếp hạng sản phẩm theo số lượng bán.'),
    bullet('**Phân bố đơn hàng:** Pie/Donut chart phân bố theo trạng thái đơn và phương thức thanh toán.'),
    spacer(),

    // ===== 4.3 =====
    h2('4.3. Phát triển website (Backend)'),
    p('Backend AuraPC được xây dựng trên nền tảng Node.js với Express.js 5.2, kết nối MongoDB thông qua Mongoose 9.1. Kiến trúc backend tuân theo mô hình MVC (Model-View-Controller) tách biệt với RESTful API.', { indent: true }),

    p('**Kiến trúc tổng quan:**', { indent: true }),
    makeTable(
      [2400, 2000, 4960],
      ['Thành phần', 'Công nghệ', 'Vai trò'],
      [
        ['Runtime', 'Node.js 22+', 'Môi trường thực thi JavaScript phía server'],
        ['Framework', 'Express.js 5.2', 'HTTP routing, middleware pipeline, error handling'],
        ['Database', 'MongoDB Atlas + Mongoose 9.1', '20 models, validation, middleware hooks'],
        ['Auth', 'JWT (7 ngày) + bcrypt', 'User OTP auth + Admin email/password auth'],
        ['Real-time', 'Socket.IO', 'Order updates, support chat, notifications'],
        ['Payment', 'MoMo SDK + ZaloPay API', 'HMAC-SHA256 signature, IPN callback'],
        ['AI', 'Replicate API (Qwen 3 235B)', 'Chatbot product recommendations'],
        ['Email', 'Nodemailer (Gmail SMTP)', 'Order invoices, builder PDF email'],
        ['PDF', 'PDFKit', 'Invoice PDF, builder config PDF'],
        ['File Upload', 'Multer', 'Avatar, hub images (10MB limit)'],
        ['Logging', 'Pino', 'Structured logging (pretty dev, JSON prod)'],
        ['Deployment', 'Render.com (Singapore)', 'Auto-deploy from GitHub main branch'],
      ]
    ),
    spacer(),

    p('**Cấu trúc thư mục Backend:**', { indent: true }),
    bullet('**server/index.js:** Entry point — khởi tạo Express, load routes, CORS whitelist, error handler, Socket.IO, health check.'),
    bullet('**server/config/database.js:** Kết nối MongoDB Atlas với retry logic (5 lần, exponential backoff).'),
    bullet('**server/middleware/auth.js:** JWT authentication — signToken, verifyToken, requireAuth, optionalAuth, requireAdmin, requireUserOrAdmin.'),
    bullet('**server/models/ (20 files):** Mongoose schemas với validation, indexes, virtual fields, TTL.'),
    bullet('**server/routes/ (16 files):** Customer-facing API routes (auth, products, orders, cart, payment, reviews, hub, chat, support, promotions, blogs, faqs, notifications, builders, categories).'),
    bullet('**server/routes/admin/ (11 files):** Admin API routes (auth, dashboard, products, categories, blogs, orders, users, notifications, support, hub, promotions).'),
    bullet('**server/utils/ (12 files):** Utilities — email, invoicePdf, buildPdf, momo, zalopay, logger, productFilters, productNormalizers, filterOptionsConfig, adminNotifications, userNotifications, supportChat.'),
    bullet('**server/socket.js:** Socket.IO server — room-based (user:userId, admin), JWT auth on connection, event emitters for order updates, support chat.'),
    spacer(),

    p('**Luồng xử lý request tiêu biểu (Tạo đơn hàng):**', { indent: true }),
    numberedItem('Client gửi POST /api/orders với body: { items, shippingAddress, paymentMethod, promotionCode, requestInvoice }.'),
    numberedItem('Express router match → optionalAuth middleware (kiểm tra JWT nếu có, set req.userId).'),
    numberedItem('Route handler: Validate input → Kiểm tra stock từng sản phẩm (Product.findById) → Tính giá server-side (chống manipulation).'),
    numberedItem('Nếu có promotionCode: Validate promotion (ngày hiệu lực, giới hạn sử dụng, đơn tối thiểu) → Tính discount.'),
    numberedItem('Kiểm tra trùng lặp đơn hàng (15 phút window, fingerprint: paymentMethod + total + items).'),
    numberedItem('Tạo Order document → Giảm stock sản phẩm (Product.updateOne, $inc: -qty) → Xóa sản phẩm khỏi Cart.'),
    numberedItem('Tạo PromotionUsage (nếu có KM) → Tạo UserNotification + AdminNotification.'),
    numberedItem('Socket.IO: emitOrderUpdated() → Gửi đến room "admin" và "user:userId".'),
    numberedItem('Nếu requestInvoice: Tạo PDF invoice (PDFKit) → Gửi email kèm PDF (Nodemailer).'),
    numberedItem('Response: 201 Created với order data → Frontend redirect đến trang thành công.'),
    spacer(),

    p('**Bảo mật Backend:**', { indent: true }),
    bullet('JWT_SECRET bắt buộc — server crash nếu thiếu (không sử dụng fallback secret).'),
    bullet('Giá sản phẩm được verify server-side trong mọi route thanh toán (chống client-side price manipulation).'),
    bullet('MoMo/ZaloPay callback sử dụng HMAC-SHA256 signature verification.'),
    bullet('OTP devOtp chỉ hiển thị trong NODE_ENV=development.'),
    bullet('Duplicate order detection với rolling window 15 phút.'),
    bullet('CORS whitelist-based: Chỉ cho phép các origin đã đăng ký (Vercel domains + localhost).'),
    bullet('Pending payments tự động cleanup sau 30 phút (in-memory store).'),
  ];
};
