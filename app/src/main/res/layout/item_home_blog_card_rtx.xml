<?xml version="1.0" encoding="utf-8"?>
<LinearLayout xmlns:android="http://schemas.android.com/apk/res/android"
    android:layout_width="240dp"
    android:layout_height="264dp"
    android:layout_marginEnd="16dp"
    android:background="@drawable/bg_home_section_card"
    android:clipToOutline="true"
    android:elevation="2dp"
    android:orientation="vertical">

    <ImageView
        android:layout_width="match_parent"
        android:layout_height="128dp"
        android:scaleType="centerCrop"
        android:src="@drawable/figma_blog_rtx" />

    <LinearLayout
        android:layout_width="match_parent"
        android:layout_height="0dp"
        android:layout_weight="1"
        android:orientation="vertical"
        android:padding="16dp">

        <TextView
            android:layout_width="match_parent"
            android:layout_height="15dp"
            android:ellipsize="end"
            android:fontFamily="sans-serif-medium"
            android:includeFontPadding="false"
            android:maxLines="1"
            android:text="@string/home_blog_date_two"
            android:textColor="@color/aura_muted"
            android:textSize="10sp" />

        <TextView
            android:layout_width="match_parent"
            android:layout_height="40dp"
            android:layout_marginTop="8dp"
            android:ellipsize="end"
            android:fontFamily="sans-serif-bold"
            android:lineSpacingExtra="1dp"
            android:maxLines="2"
            android:text="@string/home_blog_title_two"
            android:textColor="@color/aura_ink"
            android:textSize="14sp" />

        <TextView
            android:layout_width="match_parent"
            android:layout_height="36dp"
            android:layout_marginTop="6dp"
            android:ellipsize="end"
            android:lineSpacingExtra="1dp"
            android:maxLines="2"
            android:text="@string/home_blog_excerpt_two"
            android:textColor="@color/aura_muted"
            android:textSize="11sp" />
    </LinearLayout>
</LinearLayout>
