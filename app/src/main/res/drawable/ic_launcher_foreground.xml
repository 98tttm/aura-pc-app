<vector xmlns:android="http://schemas.android.com/apk/res/android"
    android:width="108dp"
    android:height="108dp"
    android:viewportWidth="108"
    android:viewportHeight="108">
    <!-- Adaptive icon safe zone: viewport 66x66 dp centered -->
    <!-- Aura logo: stylized "A" + diamond on white, drawn over aura_orange background -->

    <!-- Diamond (gold) -->
    <path
        android:fillColor="#FFD700"
        android:pathData="M54,32 L60,38 L54,44 L48,38 Z"/>

    <!-- Stylized "A" letter (white), centered around (54, 56) -->
    <!-- Outer A triangle outline -->
    <path
        android:fillColor="#FFFFFFFF"
        android:pathData="M54,42 L72,82 L36,82 Z M54,52 L66,78 L42,78 Z"
        android:fillType="evenOdd"/>

    <!-- Horizontal bar of A (the crossbar) cut-out effect via overlay (orange) -->
    <path
        android:fillColor="#FFFF6B00"
        android:pathData="M46,68 L62,68 L62,72 L46,72 Z"/>

    <!-- Small apex accent -->
    <path
        android:fillColor="#FFFFFFFF"
        android:pathData="M54,38 L56,42 L52,42 Z"/>
</vector>
